# mysql-docker-compose-with-import-sql
mysql docker compose with import sql files

add your sql files in the data directory

Change root password and databasename to whatever you wish

docker-compose up

